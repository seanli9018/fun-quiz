# Database
# PostgreSQL connection string — on Railway this is injected automatically
DATABASE_URL="postgresql://user:password@localhost:5432/fun-quiz"

# Better Auth
# Generate a strong secret: `openssl rand -base64 32`
BETTER_AUTH_SECRET="your-secret-here"

# The public URL of your app (used for auth redirects)
# Locally: http://localhost:3000
# On Railway: https://your-app.railway.app
BETTER_AUTH_URL="http://localhost:3000"
